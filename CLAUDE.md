# エージェント向けルール

## コマンド実行前の確認

Bash/PowerShellでコマンドを実行する、またはファイルを作成・編集・削除する**直前に必ず**、日本語1行で以下を明示してから実行すること。

- 何をするか
- 「読み取り専用(確認のみ)」か「変更・削除を伴う」かを明確に区別する
- 予告と操作は1対1にする。1回の予告で複数の操作をまとめない。

読み取り専用の場合は「確認だけ」等と明示し、変更・削除を伴う場合はそれとわかるように書く。まとまった方針変更に着手する前は、個々のコマンドの前だけでなく全体像も先に共有する。

